# Contracts

This package is for shared frontend/backend contracts.

Recommended next steps:

- Add `openapi.yaml` for HTTP endpoints.
- Generate frontend TypeScript types from the OpenAPI schema.
- Keep request/response names stable before moving implementation.
